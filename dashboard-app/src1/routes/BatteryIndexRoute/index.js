//-----------  Imports  -----------//

import { connect } from 'react-redux';

import BatteryIndexRoute from './BatteryIndexRoute';

//-----------  Redux Maps  -----------//

const mapState = state => ({});

const mapDispatch = dispatch => ({});

//-----------  Exports  -----------//

export default connect(
  mapState,
  mapDispatch,
)(BatteryIndexRoute);
