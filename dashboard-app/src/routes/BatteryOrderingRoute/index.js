//-----------  Imports  -----------//

import { connect } from 'react-redux';

import BatteryOrderingRoute from './BatteryOrderingRoute';

//-----------  Redux Maps  -----------//

const mapState = state => ({});

const mapDispatch = dispatch => ({});

//-----------  Exports  -----------//

export default connect(
  mapState,
  mapDispatch,
)(BatteryOrderingRoute);
