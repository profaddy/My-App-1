//-----------  Imports  -----------//

import { connect } from 'react-redux';

import BatteryPreviewRoute from './BatteryPreviewRoute';

//-----------  Redux Maps  -----------//

const mapState = state => ({});

const mapDispatch = dispatch => ({});

//-----------  Exports  -----------//

export default connect(
  mapState,
  mapDispatch,
)(BatteryPreviewRoute);
