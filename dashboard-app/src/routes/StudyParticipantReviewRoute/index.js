//-----------  Imports  -----------//

import { connect } from 'react-redux';

import StudyParticipantReviewRoute from './StudyParticipantReviewRoute';

//-----------  Redux Maps  -----------//

const mapState = state => ({});

const mapDispatch = dispatch => ({});

//-----------  Exports  -----------//

export default connect(
  mapState,
  mapDispatch,
)(StudyParticipantReviewRoute);
