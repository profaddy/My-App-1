//-----------  Imports  -----------//

import { connect }         from 'react-redux'

import QuestionnairesRoute from './QuestionnairesRoute'

//-----------  Redux Maps  -----------//

const mapState = (state) => ({})

const mapDispatch = (dispatch) => ({})

//-----------  Exports  -----------//

export default connect(mapState, mapDispatch)(QuestionnairesRoute)
